extern const unsigned int MAX_FRUIT_NUM;
extern const int MAX_LENGTH;

// A struct that contains all the information about a type of fruit.
struct Fruit
{
   char  name[MAX_LENGTH];
   float unit_price;
   int   total_count;
   int   sold_count;
};

// Print the name and count of each kind of fruit
void stock_checking(const Fruit , int){   
}

// Update stock info and print (name, total_count) pairs of each kind of fruit.
void stock_updating(Fruit , int , int ){
}

// Calculate selling amount
float selling_amount_calculating(const Fruit , int ){

}